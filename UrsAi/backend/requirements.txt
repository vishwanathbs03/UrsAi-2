fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
pydantic[email]==2.10.3
python-dotenv==1.0.1
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0
httpx==0.28.1

# --- Authentication (Sprint 1 Part 3) ---
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
email-validator==2.2.0

# --- OCR (multipart upload support, transitively required by FastAPI) ---
# Required at import time by the OCR endpoint's `File(...)` / `UploadFile`
# form-data parameters. Not a feature change; just pins the version the
# dev venv was using so the production image boots without surprise
# missing-dep failures.
python-multipart==0.0.20
